
import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';

export default {
  plugins: [
    tailwindcss,  // Usa il plugin tailwindcss importato
    autoprefixer, // Usa il plugin autoprefixer importato
  ],
};
